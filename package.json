{
  "name": "wallpaper-setter-workspace",
  "private": true,
  "scripts": {
    "dev": "npm --prefix electron run dev",
    "build": "npm --prefix electron run build",
    "test": "npm --prefix electron run test",
    "package": "npm --prefix electron run package",
    "release:local": "./scripts/build-app.sh && ./scripts/package-dmg.sh"
  }
}
